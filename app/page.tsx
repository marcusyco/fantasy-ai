export default function HomePage() {
  return (
    <main>
      <h1>Fantasy AI</h1>
      <p>
        This deployment has no dashboard UI yet — it&apos;s an API-only backend. See the
        project README for the API routes (Yahoo OAuth, Linq webhook, cron jobs) and how to
        wire up a league.
      </p>
    </main>
  );
}
